# HJATS 环境变量配置
# 复制此文件为 .env 并填入你的API密钥

# Binance 合约 API 配置
BINANCE_API_KEY=your_binance_api_key_here
printf '%s\n' '' '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
ubuntu@VM-0-14-ubuntu:~/HJATS$
         printf '%
         s\n' ''printf '%s\n' '' '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
         ubuntu@VM-0-14-ubuntu:~/HJATS$
                  printf '%
                  s\n' '' '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
                printf '%s\n' '' '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
                ubuntu@VM-0-14-ubuntu:~/HJATS$
                         printf '%
                         s\n' '' '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
                         ubuntu@VM-0-14-ubuntu:~/HJATS$
                            ubuntu@VM-0-14-ubuntu:~/HJATS$
                    '    def test_death_cross(self, sample_klines_50):' '        """MA10 down-cross MA30 -> signal=-1"""' '        df = sample_klines_50.copy()' '        df["close"] = np.concatenate([np.linspace(2000,2050,30), np.linspace(2040,1900,20)])' '        result = signalAlg.run({"dataset": df})' '        signal, _, _ = result' '        assert signal == -1, f"Expected -1, got {signal}"' >> /home/ubuntu/HJATS/tests/test_signal.py
         ubuntu@VM-0-14-ubuntu:~/HJATS$
          =your_binance_api_secret_here